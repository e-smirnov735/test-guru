<div>
  <%= render 'shared/errors', resource: @question %>

  <%= form_with model: [:admin, @test, @question], local: true do |form| %>
    <p>
      <%= form.label :body, "Название" %><br>
      <%= form.text_field :body %>
    </p>
    <%= form.submit title %>
  <% end %>
</div>
